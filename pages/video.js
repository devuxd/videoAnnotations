import Layouts from "./layouts";
import Video from "../components/video";
const vid = () => (
  <div style={{ fontFamily: "Lato" }}>
    <Layouts>
      <Video />
    </Layouts>
  </div>
);

export default vid;
